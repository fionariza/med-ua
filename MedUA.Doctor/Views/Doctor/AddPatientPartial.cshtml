@model MedUA.Models.AddPatientViewModel

@using (Html.BeginForm("AddPatient", "Doctor", FormMethod.Post))
{
    @Html.HiddenFor(model=>model.Id)
    <input type="submit" class="btn btn-default" value=@MedUA.Resources.Resource.AddPatientPartialButton />
}
